# ProGuard rules for MathWise release builds.
# Intentionally minimal: Flutter plugins already ship their own ProGuard configs.
# Add project-specific keep rules here if minification breaks runtime behavior.
